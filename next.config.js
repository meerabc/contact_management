/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  // Disable Tailwind CSS
  // Custom CSS support through regular imports
};

module.exports = nextConfig;
